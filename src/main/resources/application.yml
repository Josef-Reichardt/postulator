spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/opengeodb"
    username: "root"
    password: "pass"

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
